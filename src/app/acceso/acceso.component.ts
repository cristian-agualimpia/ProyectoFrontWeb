import { Component } from '@angular/core';

@Component({
  selector: 'app-acceso',
  standalone: true,
  imports: [],
  templateUrl: './acceso.component.html',
  styleUrl: './acceso.component.css'
})
export class AccesoComponent {
  /*
  constructor(private router: Router) {}

  onSubmit(form: HTMLFormElement) {
    const role = form.role.value; // Obtener el valor del rol seleccionado
    if (role === 'arrendador') {
      this.router.navigate(['/arrendador']); // Navegar al dashboard del arrendador
    } else {
      this.router.navigate(['/arrendatario']); // Navegar al dashboard del arrendatario
    }
  } 
  */
}
